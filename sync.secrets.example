# Скопируйте:  cp sync.secrets.example sync.secrets
# Файл sync.secrets в репозиторий НЕ коммитится (.gitignore).
#
# Для push/pull по HTTPS скрипт подставит токен через git credential (локально для этого репо).

GITHUB_USER="ваш_логин_github"
GITHUB_TOKEN="ghp_вставьте_токен_сюда"
